/**
 * Deze klasse wordt gebruikt om de brandstof in de lijst 'mogelijkeBrandstoffen' op te bouwen voor het verzenden naar de Backend.
 */
export interface IBrandstof {
      id: string;
      typeBrandstof: string;
}
